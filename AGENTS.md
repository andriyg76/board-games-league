# AGENTS.md - Документація для AI агентів

Цей документ містить важливу інформацію про архітектуру проекту, принципи розробки та контекст для продовження роботи.

## 🎯 Принципи розробки

### 1. Розділення ID та Code

**HTTP Layer (handlers):**
- Працює **виключно з Code** (string)
- Не має в значеннях нічого що називається `ID`
- Отримує `IdAndCode` з контексту або через кеш

**Service Layer:**
- Має приймати `IdAndCode` замість `primitive.ObjectID`
- Поточне використання `primitive.ObjectID` має бути замінене на `IdAndCode`
- Використовує `IdAndCode.ID` для передачі в Repository Layer

**Repository Layer:**
- Працює тільки з `primitive.ObjectID`
- Конвертація IdAndCode.ID → ObjectID при викликах репозиторіїв

**Структура IdAndCode:**
```go
type IdAndCode struct {
    ID   primitive.ObjectID
    Code string
}
```

### 2. Мінімальні бізнес-інтерфейси

**Принцип:** Інтерфейси мають містити тільки методи, необхідні для бізнес-логіки.

**Приклад:**
```go
// ✅ Правильно - мінімальний інтерфейс
type IdAndCodeCache interface {
    GetByID(id primitive.ObjectID) *models.IdAndCode
    GetByCode(code string) (*models.IdAndCode, error)
}

// ❌ Неправильно - занадто багато методів
type IdAndCodeCache interface {
    GetByID(...)
    GetByCode(...)
    Store(...)
    Remove(...)
    Clear(...)
    CleanExpired(...)
    GetStats(...)
}
```

**Додаткові методи** (cleanup, stats, invalidation) передаються додатковими інтерфейсами або функціями в підсистеми, які їх викликають. Наприклад, `CacheCleanupService` отримує кеші через інтерфейс `cache.CleanableCache`, а не через бізнес-інтерфейс `IdAndCodeCache`.

### 3. Кешування

**Архітектура кешів:**
- LRU політика витіснення
- TTL для автоматичного застарівання
- Обмеження розміру:
  - IdAndCode: 2000 записів
  - League, Membership, User: по 500 записів кожен
- Хуки очищення:
  - За точним ключем
  - Функція `(*ключ): boolean`
  - Функція `(*значення): boolean`

**Кеші що використовуються:**
- `IdAndCodeCache` - пари ID-Code
- `LeagueCache` - об'єкти League
- `MembershipCache` - об'єкти LeagueMembership
- `UserCache` - об'єкти User

**Реєстрація кешів:**
- Кеші реєструються в `CacheCleanupService` автоматично при створенні
- Cleanup service періодично очищає застарілі записи (за замовчуванням кожні 15 хвилин)
- Статистика кешів експозиться через diagnostic API

### 4. Middleware та контекст

**League Middleware:**
- Використовує **сервіси** для отримання об'єктів (не кеші напряму)
- Сервіси прозоро кешують об'єкти
- Middleware також прозоро кешує через сервіси
- Додає в контекст: `leagueIdAndCode`, `userIdAndCode`, `league`, `membership`
- Handlers отримують готові об'єкти з контексту, не парсять URL

**Auth Middleware:**
- Встановлює user profile в контекст
- Може також встановлювати `userIdAndCode` через сервіси (які кешують прозоро)


### 1. Про IdAndCode та конвертацію
**Моє розуміння:** 
- HTTP layer працює тільки з Code (string)
- IdAndCode структура містить обидва значення синхронно
- Конвертація відбувається через кеш, який зберігає пари ID-Code


### 2. Про мінімальні інтерфейси
- Бізнес-інтерфейси містять тільки методи, необхідні для бізнес-логіки
- Додаткові методи (cleanup, stats, invalidation) передаються через окремі інтерфейси або функції в підсистеми, які їх викликають
- Наприклад, `CacheCleanupService` працює з `cache.CleanableCache`, а не з бізнес-інтерфейсом `IdAndCodeCache`
- Це дозволяє зберігати бізнес-інтерфейси простими та зрозумілими

**Відповідь:** Зараз інвалідація має бути через окремі інтерфейси/функції. Можуть бути випадки в майбутньому, коли потрібно буде додавати методи інвалідації в бізнес-інтерфейси, але поки про них не говоримо.

### 3. Про кешування в middleware
**Моє розуміння:**
- Middleware використовує **сервіси** для отримання об'єктів (не кеші напряму)
- Сервіси прозоро кешують об'єкти (через обгортку або вбудоване кешування)
- Middleware також прозоро кешує через сервіси
- Handlers отримують готові об'єкти з контексту

**Відповідь:** Middleware має кешувати прозоро і сервіси. Middleware має використовувати сервіси (не кеші напряму).

### 4. Про реєстрацію кешів
**Моє розуміння:**
- Кеші реєструються в CacheCleanupService автоматично при створенні
- Cleanup service автоматично очищає застарілі записи
- Статистика збирається через GetAllStats()

**Відповідь:** Кеші мають реєструватися автоматично при створенні (не в main.go при ініціалізації).

### 5. Про інвалідацію кешу
**Моє розуміння:**
- При оновленні об'єктів потрібно інвалідувати кеш
- Використовуються хуки очищення (Remove, RemoveByKeyFunc, RemoveByValueFunc)
- Це відбувається в сервісах при оновленнях

**Відповідь:** Інвалідація кешу має бути частиною операцій оновлення (не окремими методами). Наприклад, при виклику `UpdateLeague()` автоматично інвалідується кеш League.

## 🚀 Наступні кроки (після підтвердження)

1. Інтеграція кешів в middleware (league_middleware.go, auth.go)
2. Створення прозорої обгортки кешування для сервісів
3. Інвалідація кешів при оновленнях
4. Тестування та оптимізація

## 📝 Нотатки

- Всі кеші мають thread-safe реалізацію
- TTL налаштування: IdAndCode - 1 година, об'єкти - 30 хвилин
- Cleanup interval: 15 хвилин (налаштовується)
- Статистика доступна через `/api/admin/diagnostics` (тільки для superadmin)

### Логування HTTP помилок

- Для **5xx/неочікуваних** помилок у handlers/middleware використовувати `utils.LogAndWriteHTTPError` (з `*http.Request`) щоб мати лог із контекстом.
- Для очікуваних 4xx можна залишати `http.Error`, якщо лог не потрібен.

## 📦 MongoDB Прив'язки полів (BSON теги)

### Правила написання BSON тегів

**Загальні принципи:**
1. **Назви полів в MongoDB використовують snake_case**
2. **Всі поля мають bson теги** (окрім випадків, коли поле не зберігається в БД)
3. **JSON теги додаються тільки для полів, які експортуються в API**

### Конвенції назв полів

**ID поля:**
- `_id` - головний ідентифікатор (завжди `bson:"_id,omitempty"`)
- `{entity}_id` - посилання на іншу сутність (наприклад: `league_id`, `user_id`, `game_round_id`, `membership_id`)
- `created_by`, `used_by` - посилання на користувача, який виконав дію

**Дата/час поля:**
- `created_at` - дата створення (завжди присутня)
- `updated_at` - дата оновлення (завжди присутня)
- `{action}_at` - дата дії (наприклад: `joined_at`, `used_at`, `expires_at`)
- `{action}_time` - час дії (наприклад: `start_time`, `end_time`)
- `last_{action}_at` - остання дія (наприклад: `last_activity_at`)

**Статуси та прапорці:**
- `status` - статус сутності (без суфіксу)
- `is_{state}` - булеві прапорці (наприклад: `is_used`, `is_read`, `is_moderator`)

**Інші поля:**
- Використовується snake_case для всіх полів
- Складні назви: `player_alias`, `recent_co_players`, `cooperative_score`

### Опціональні поля

**Коли використовувати `,omitempty`:**
- ✅ Поля, які можуть бути порожніми/нульовими: `bson:"field,omitempty"`
- ✅ Посилання на інші сутності, які можуть бути відсутні: `bson:"invitation_id,omitempty"`
- ✅ Додаткові метадані: `bson:"alias,omitempty"`
- ❌ Обов'язкові поля: без `omitempty`
- ❌ `_id` завжди має `omitempty`: `bson:"_id,omitempty"`

### Спеціальні випадки

**Поле `Code`:**
```go
Code string `bson:"-" json:"code"`
```
- `bson:"-"` означає, що поле **не зберігається** в MongoDB
- Заповнюється через `idCodeCache` при серіалізації в JSON
- Завжди має JSON тег для експорту в API

**Поля, які не експортуються в JSON:**
```go
LeagueID primitive.ObjectID `bson:"league_id,omitempty" json:"-"`
```
- Використовується для внутрішніх посилань
- Не повинні бути доступні через API

**JSON теги:**
- Додаються тільки для моделей, які експортуються в API
- Використовується `camelCase` або `snake_case` залежно від контексту API
- Для полів, які не повинні експортуватися: `json:"-"`

### Приклади

**✅ Правильно:**
```go
type League struct {
    ID        primitive.ObjectID `bson:"_id,omitempty"`
    Version   int64              `bson:"version"`
    Name      string             `bson:"name"`
    Status    LeagueStatus       `bson:"status"`
    CreatedAt time.Time          `bson:"created_at"`
    UpdatedAt time.Time          `bson:"updated_at"`
}

type GameRound struct {
    ID               primitive.ObjectID `bson:"_id,omitempty" json:"id,omitempty"`
    Code             string             `bson:"-" json:"code"`
    LeagueID         primitive.ObjectID `bson:"league_id,omitempty" json:"-"`
    Name             string             `bson:"name" json:"name"`
    StartTime        time.Time          `bson:"start_time" json:"start_time"`
    EndTime          time.Time          `bson:"end_time" json:"end_time,omitempty"`
    CreatedAt        time.Time          `bson:"created_at" json:"created_at,omitempty"`
}
```

**❌ Неправильно:**
```go
// Неправильно: camelCase в bson
Name string `bson:"name"`  // ❌ має бути snake_case

// Неправильно: відсутній omitempty для опціонального поля
InvitationID primitive.ObjectID `bson:"invitation_id"`  // ❌ має бути `,omitempty`

// Неправильно: Code зберігається в БД
Code string `bson:"code"`  // ❌ має бути `bson:"-"`
```

### Чеклист при створенні нової моделі

- [ ] Всі поля мають bson теги з snake_case
- [ ] `_id` має `bson:"_id,omitempty"`
- [ ] Опціональні поля мають `,omitempty`
- [ ] Дати мають суфікси `_at` або `_time`
- [ ] Посилання на інші сутності мають суфікс `_id`
- [ ] `Code` поле має `bson:"-"` (якщо використовується)
- [ ] JSON теги додані для полів, які експортуються в API
- [ ] Внутрішні поля мають `json:"-"`

## 🎨 UI/UX Особливості

### Меню ліг (LeagueMenuItem)

**Компонент:** `frontend/src/components/LeagueMenuItem.vue`

**Функціональність:**
- Динамічне dropdown меню зі списком ліг
- Поточна вибрана ліга відображається першою з галочкою (✓)
- Для суперадміна:
  - Меню завжди видиме (навіть якщо 0-1 ліга)
  - Показуються всі ліги (активні + архівні)
  - Архівні ліги позначаються як "(Архівна)"
- Для звичайних користувачів:
  - Меню приховане, якщо доступна тільки одна ліга або немає ліг
- В кінці меню додано роздільник та пункт "Ліги" для переходу на сторінку всіх ліг

**Логіка відображення:**
- `shouldShowMenu`: `userStore.isSuperAdmin || activeLeagues.length > 1`
- `menuOptions`: для суперадміна - всі ліги, для інших - тільки активні

### Автоматичний перехід на лігу

**Компонент:** `frontend/src/views/HomeView.vue`

**Поведінка при відкритті головної сторінки (`/`):**
1. Завантажує список ліг
2. Перевіряє, чи користувач залогінений
3. Якщо залогінений і є ліги:
   - Перевіряє збережену лігу (localStorage)
   - Якщо збережена ліга існує і активна → перехід на неї
   - Якщо ні → перехід на першу активну лігу
4. Якщо залогінений, але немає ліг → показує повідомлення
5. Якщо не залогінений → показує головну сторінку зі статистикою

**Навігація:**
- Окремий пункт "Ліги" видалено з desktop навігації
- Пункт "Ліги" доступний через dropdown меню ліг (в кінці списку)
- В мобільному меню пункт "Ліги" залишено окремо

